# SSH passwords for deploy-region-latency-probes.sh (do NOT commit real values).
# Copy to .env.probe on the back host: /opt/testVPStrade/infra/docker/.env.probe
NL_SSH_PASS=
DE_SSH_PASS=
FI_SSH_PASS=
GB_SSH_PASS=
CERTBOT_EMAIL=admin@cloud-hustle.com
